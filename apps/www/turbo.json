{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "generate:registry": {},
    "generate:themes": {},
    "dev": {
      "dependsOn": ["generate:themes"]
    },
    "build": {
      "dependsOn": ["^build", "generate:registry", "generate:themes"]
    }
  }
}
