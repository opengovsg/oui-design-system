---
title: MultiSelect
description: "Not a component in OUI"
category: Form & Input
toc:
  visible: false
---

The `MultiSelect` component is not available in OUI.

<br />
For multi-selection functionality, consider using:

- the [`TagField`](/docs/components/tag-field) component for a component that allows selection and filtering via text input.
