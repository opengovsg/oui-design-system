import type { VariantProps } from "tailwind-variants"

import { tv } from "../utils/tv"

export const {{ camelCase name }}Styles = tv({
  base: [],
  variants: {
    variant: {},
    size: {},
  },
  defaultVariants: {},
})

export type {{ pascalCase name }}VariantProps = VariantProps<typeof {{ camelCase name }}Styles>
