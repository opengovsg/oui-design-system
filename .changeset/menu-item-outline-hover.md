---
"@opengovsg/oui-theme": patch
---

Suppress the default browser focus outline on menu and list box items so the focus-follows-mouse behaviour no longer shows a focus ring on hover. A focus ring now only appears on keyboard focus via `focus-visible`; mouse hover continues to use the background highlight.
