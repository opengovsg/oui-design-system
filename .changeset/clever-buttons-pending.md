---
"@opengovsg/oui": patch
---

Add `preserveWidth` prop to `Button` to prevent layout shift while pending by keeping the children in the layout (visually hidden) and overlaying the spinner over them.
